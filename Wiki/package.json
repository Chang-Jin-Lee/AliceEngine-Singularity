{
  "name": "alice-engine-wiki",
  "version": "0.1.0",
  "private": true,
  "description": "AliceEngine-Singularity 문서 사이트. 사람과 AI가 같은 소스를 읽는다.",
  "scripts": {
    "predev": "npm run generate",
    "dev": "next dev",
    "prebuild": "npm run generate",
    "build": "next build",
    "start": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "node --test scripts/*.test.mjs",
    "generate": "node scripts/generate.mjs && node scripts/mirror.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^14.1.4",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
