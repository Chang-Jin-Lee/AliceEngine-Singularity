# CHR-10 · 현재 기능 안내와 로컬 위키 실행

```
상태: 진행중
크기: M
선행: 없음
담당: Codex (Chrono)
```

## 무엇을

README와 위키에 Phase 0에서 실제 실행되는 기능을 안내하고, 정적 빌드한 위키를
로컬 브라우저에서 볼 수 있게 한다. 기존 사이트와 생성 스크립트를 사용한다.

## 왜

문서 검증 CLI와 게임 런타임을 구분해야 사용자가 현재 엔진을 직접 시험할 수 있다.
`npm start`는 정적 내보내기 설정과 충돌하고, `Wiki/lib/content.js`는 전역 `lib/`
무시 규칙으로 저장소에서 빠져 있다. 다른 컴퓨터에서도 같은 위키가 빌드되어야 한다.

## 완료 기준

- [ ] README·위키 첫 화면·시작 가이드가 CLI와 런타임의 현재 상태를 구분한다
- [ ] 필요한 위키 소스가 Git 추적 대상이다
- [ ] 엔진이 없는 환경에서도 저장된 API 데이터로 전체 레퍼런스를 재생성한다
- [ ] 정적 빌드와 로컬 미리보기, HTML·마크다운·JSON 접근을 검증한다
- [ ] 데스크톱과 모바일 화면을 확인한다
- [ ] 실제 CLI 및 Windows 빌드 결과와 실행 방법을 기록한다

## 건드리는 파일

```
README.md
Docs/STATUS.md
Wiki/
.gitignore
Scripts/build.ps1 (검증 중 확인한 VS 개발자 환경 경로 오류)
```

## 범위

Vercel 공개 배포, 검색, 영어 번역은 CHR-07에 남는다.
ECS와 실제 그래픽 백엔드 구현은 각 역할의 기존 백로그에 남는다.
