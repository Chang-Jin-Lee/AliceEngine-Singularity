# A text-only playground for the editor's ECS transform preview.
schema: alice/project/1
name: CubePlayground
version: 0.1.0
startScene: scenes/main.scene.yaml
contentRoot: .
renderer: { backend: "null", targetFrameRate: 60, resolution: [1600, 900] }
platforms:
  - windows
